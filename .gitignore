# scraped raw data and downloaded images are large and reproducible (see README)
data/raw/
data/images/
data/*.log
data/images-missing.txt
node_modules/
.DS_Store
data/*.sh
data/*.out
# built databases are committed explicitly once final (see README); intermediate builds are ignored
app/assets/db/*.db
